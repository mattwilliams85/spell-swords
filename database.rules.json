{
"rules": {
    ".read": true,
    ".write": true
  }
}


{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        "medications": {
          "$medication_id": {

          }
        }
      }
    }
  }
}
